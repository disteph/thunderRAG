# OCaml Server Test Data

This directory contains data files used by the OCaml integration test suite
(`test/run_tests.exe`) and quality harness (`test/quality_harness.exe`).

- `corpus.json` — synthetic email corpus for quality tests
- `test_cases.json` — scored question battery for quality tests
- `runs/` — timestamped output from quality harness runs
